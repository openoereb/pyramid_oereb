<%page args="datasource"/>
<xsd:datasource>
    <xsd:topic>
        <%include file="theme.xml" args="theme=datasource.theme"/>
    </xsd:topic>
    <xsd:dataownerName>${localized(datasource.owner.name).get('Text')}</xsd:dataownerName>
    <xsd:transferFromSource>${datasource.date}</xsd:transferFromSource>
</xsd:datasource>