<%page args="office"/>
<Name>
    <%include file="multilingual_text.xml" args="text=office.name"/>
</Name>
%if office.office_at_web:
<OfficeAtWeb>${office.office_at_web|u}</OfficeAtWeb>
%endif
%if office.uid:
<UID>${office.uid}</UID>
%endif
%if office.line1:
<Line1>${office.line1}</Line1>
%endif
%if office.line2:
<Line2>${office.line2}</Line2>
%endif
%if office.street:
<Street>${office.street}</Street>
%endif
%if office.number:
<Number>${office.number}</Number>
%endif
%if office.postal_code:
<PostalCode>${office.postal_code}</PostalCode>
%endif
%if office.city:
<City>${office.city}</City>
%endif
