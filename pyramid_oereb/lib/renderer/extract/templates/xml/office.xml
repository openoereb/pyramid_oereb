<%page args="office"/>
<data:Name>
    <%include file="multilingual_text.xml" args="text=office.name"/>
</data:Name>
%if office.office_at_web:
<data:OfficeAtWeb>${office.office_at_web|u}</data:OfficeAtWeb>
%endif
%if office.uid:
<data:UID>${office.uid}</data:UID>
%endif
%if office.line1:
<data:Line1>${office.line1}</data:Line1>
%endif
%if office.line2:
<data:Line2>${office.line2}</data:Line2>
%endif
%if office.street:
<data:Street>${office.street}</data:Street>
%endif
%if office.number:
<data:Number>${office.number}</data:Number>
%endif
%if office.postal_code:
<data:PostalCode>${office.postal_code}</data:PostalCode>
%endif
%if office.city:
<data:City>${office.city}</data:City>
%endif