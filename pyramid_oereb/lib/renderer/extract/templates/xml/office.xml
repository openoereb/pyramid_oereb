<%page args="office"/>
<xsd:Name>
    <%include file="multilingual_text.xml" args="text=office.name"/>
</xsd:Name>
%if office.office_at_web:
<xsd:OfficeAtWeb>${office.office_at_web|u}</xsd:OfficeAtWeb>
%endif
%if office.uid:
<xsd:UID>${office.uid}</xsd:UID>
%endif
%if office.line1:
<xsd:Line1>${office.line1}</xsd:Line1>
%endif
%if office.line2:
<xsd:Line2>${office.line2}</xsd:Line2>
%endif
%if office.street:
<xsd:Street>${office.street}</xsd:Street>
%endif
%if office.number:
<xsd:Number>${office.number}</xsd:Number>
%endif
%if office.postal_code:
<xsd:PostalCode>${office.postal_code}</xsd:PostalCode>
%endif
%if office.city:
<xsd:City>${office.city}</xsd:City>
%endif
