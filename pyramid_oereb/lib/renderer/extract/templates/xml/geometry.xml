<%page args="geometry"/>
<xsd:Geometry>
    %if params.geometry:
        <%include file="geometry_collection.xml" args="geometry=geometry.geom"/>
    %endif
    <xsd:Lawstatus>
        ## TODO: solve code here, where may it come from?
        <xsd:Code>PLACE THE CODE HERE</xsd:Code>
        <xsd:Text>${geometry.legal_state}</xsd:Text>
    </xsd:Lawstatus>
    <xsd:MetadataOfGeographicalBaseData>
        ${geometry.geo_metadata}
    </xsd:MetadataOfGeographicalBaseData>
    <xsd:ResponsibleOffice>
        <%include file="office.xml" args="office=geometry.office"/>
    </xsd:ResponsibleOffice>
</xsd:Geometry>