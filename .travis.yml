sudo: required
language: python
python:
  - "2.7"

services:
  - docker

# command to install dependencies
install:
  - make install

before_install:
  - docker pull mdillon/postgis

# command to run tests
script:
  - make checks
